{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Make Build", // We will reference this label later
            "command": "make",
            "args": [], // You can add targets here (e.g. "debug") if your Makefile needs them
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Runs the project Makefile"
        }
    ]
}